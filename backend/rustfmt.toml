# rustfmt 配置:仅使用稳定通道支持的选项,保证 `cargo fmt --check` 在 CI 上可复现。
edition = "2021"
max_width = 100
hard_tabs = false
tab_spaces = 4
newline_style = "Unix"
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
