# 三、国际化

> **对应代码**：`frontend/src/i18n/`、`frontend/src/locales/`
> **维护提示**：国际化配置变更时同步更新本文档。

## 1、概述

NavHub 支持多语言国际化，目前支持中文和英文。

## 2、配置结构

### 2.1 语言文件

```
frontend/src/i18n/
├── index.ts          # i18n 配置
├── locales/
│   ├── zh-CN.json    # 中文翻译
│   └── en-US.json    # 英文翻译
```

### 2.2 配置示例

```typescript
// frontend/src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import zhCN from './locales/zh-CN.json';
import enUS from './locales/en-US.json';

i18n.use(initReactI18next).init({
  resources: {
    'zh-CN': { translation: zhCN },
    'en-US': { translation: enUS },
  },
  lng: 'zh-CN', // 默认语言
  fallbackLng: 'zh-CN',
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

## 3、翻译文件结构

### 3.1 中文翻译示例

```json
{
  "common": {
    "ok": "确定",
    "cancel": "取消",
    "save": "保存",
    "delete": "删除",
    "edit": "编辑"
  },
  "nav": {
    "home": "首页",
    "settings": "设置",
    "profile": "个人资料"
  },
  "auth": {
    "login": "登录",
    "logout": "退出登录",
    "username": "用户名",
    "password": "密码"
  }
}
```

### 3.2 英文翻译示例

```json
{
  "common": {
    "ok": "OK",
    "cancel": "Cancel",
    "save": "Save",
    "delete": "Delete",
    "edit": "Edit"
  },
  "nav": {
    "home": "Home",
    "settings": "Settings",
    "profile": "Profile"
  },
  "auth": {
    "login": "Login",
    "logout": "Logout",
    "username": "Username",
    "password": "Password"
  }
}
```

## 4、使用方式

### 4.1 在组件中使用

```typescript
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('nav.home')}</h1>
      <button>{t('common.save')}</button>
    </div>
  );
}
```

### 4.2 带参数的翻译

```json
{
  "welcome": "欢迎，{{name}}！"
}
```

```typescript
const { t } = useTranslation();
t('welcome', { name: '用户' }); // 输出：欢迎，用户！
```

## 5、语言切换

### 5.1 手动切换

```typescript
import { useTranslation } from 'react-i18next';

function LanguageSwitcher() {
  const { i18n } = useTranslation();
  
  const changeLanguage = (lng: string) => {
    i18n.changeLanguage(lng);
  };
  
  return (
    <select onChange={(e) => changeLanguage(e.target.value)}>
      <option value="zh-CN">中文</option>
      <option value="en-US">English</option>
    </select>
  );
}
```

### 5.2 自动检测

i18n 会自动检测浏览器语言设置，并使用匹配的翻译文件。

## 6、添加新语言

### 6.1 创建翻译文件

1. 在 `frontend/src/i18n/locales/` 目录下创建新的 JSON 文件
2. 复制现有翻译文件作为模板
3. 翻译所有键值对

### 6.2 注册新语言

在 `frontend/src/i18n/index.ts` 中添加新语言：

```typescript
import jaJP from './locales/ja-JP.json';

i18n.init({
  resources: {
    'zh-CN': { translation: zhCN },
    'en-US': { translation: enUS },
    'ja-JP': { translation: jaJP },
  },
  // ...
});
```

### 6.3 更新语言切换器

在语言切换器组件中添加新语言选项。

---
- 上一篇：[02-design-tokens.md](./02-design-tokens.md)
- 下一篇：[04-modules/01-auth/01-overview.md](../04-modules/01-auth/01-overview.md)
- 返回索引：[docs/README.md](../README.md)